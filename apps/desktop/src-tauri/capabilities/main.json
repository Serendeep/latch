{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The main window may read build availability only.",
  "windows": ["main"],
  "permissions": ["allow-app-status"]
}
